import { markdownAshtml } from '../../../helpers/markdown-ashtml';
import { module, test } from 'qunit';

module('Unit | Helper | markdown ashtml');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = markdownAshtml(42);
  assert.ok(result);
});
