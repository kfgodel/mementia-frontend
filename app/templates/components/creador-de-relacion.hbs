<div class="card">
  <div class="card-content">
    <span class="card-title">Crear relacion</span>
    <div class="row">
      {{tenpines/input-text value=parametros.idNodoOrigen label="Id de origen" col="s4" }}
      {{tenpines/input-text value=parametros.tipoDeRelacion label="Tipo de relacion" col="s4" }}
      {{tenpines/input-text value=parametros.idNodoDestino label="Id de destino" col="s4" }}
    </div>

    {{#if mensajeDeFeedback}}
      <div class="row">
        <div class="col s12">
          <span>{{mensajeDeFeedback}}</span>
        </div>
      </div>
    {{/if}}
  </div>
  <div class="card-action left-align">
    {{tenpines/material-button label='Crear' action='crearRelacion' }}
  </div>
</div>
